@Thinking i would like You to design quad esp32 system. both nodes of the link would consist of two esp32 linked together with serial link (serial1). test node would use first esp32 for test antenna and second esp32 for reference antenna, both in roughly same polarisation. The reference node would use first esp32 with ordinary 1/4 antenna in v polarisation and second esp32 with ordinary 1/4 antenna in h polarisation. Design the code so it performs the sweeps transmitting using reference node , and then reversing the roles, test node becoming transmitter. Start from outputting reference node firmware, master esp32. nodes should orchestrate the test using esp-now packets - each test should have fixed time to complete after which esp-now communication can occur. the reference node should be the test orchestrator.
